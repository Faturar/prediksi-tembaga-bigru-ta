# OpenAPI Notes

Run the ML service and open:

- `http://127.0.0.1:8001/docs`
- `http://127.0.0.1:8001/openapi.json`

The documented API includes `/health`, `/api/train`, `/api/predict`, and `/api/models/{version}`.
